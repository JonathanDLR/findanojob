{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col text-center">
                <div class="col-xs-12">
                    {% for result in results %}
                    <p>{{ result }}</p>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}